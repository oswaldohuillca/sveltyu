type TCustom = string;
type TColor = 'primary' | 'secondary' | 'success' | 'danger' | 'dark' | 'white' | TCustom;

const colors:string[] = [''];

export function getColor(color: TColor, opacity: number) {
  
}